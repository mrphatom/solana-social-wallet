# Leave empty until explicitly configuring a verified live provider.
# Never commit real bot tokens, webhook secrets, private keys, seed phrases, signatures, or RPC credentials.
PORT=3000
NODE_ENV=development
